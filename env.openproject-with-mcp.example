##
# Configuration OpenProject avec support MCP
# 
# Ce fichier étend la configuration OpenProject existante avec les variables
# nécessaires pour le serveur MCP OpenProject.
#
# Variables OpenProject (héritées de la configuration existante)
# =============================================================
TAG=16-slim
OPENPROJECT_HTTPS=true
OPENPROJECT_HOST__NAME=openproject.tms-solutions.fr
PORT=8181
OPENPROJECT_RAILS__RELATIVE__URL__ROOT=
IMAP_ENABLED=false
DATABASE_URL=postgres://postgres:p4ssw0rd@db/openproject?pool=20&encoding=unicode&reconnect=true
RAILS_MIN_THREADS=4
RAILS_MAX_THREADS=16
PGDATA="pgdata"
OPDATA="opdata"

# Variables MCP OpenProject Server
# ================================
# API Key OpenProject (40 caractères depuis votre profil OpenProject)
# IMPORTANT: Remplacez par votre vraie clé API
OPENPROJECT_MCP_API_KEY=0d70bf3282b8da3fcc7f2714cc79dafc8c17d3469275aabcb85c71592027270a

# Configuration du serveur MCP
MCP_LOG_LEVEL=INFO

# Ports externes pour accès au serveur MCP
# Ces ports seront exposés sur l'hôte pour permettre l'accès aux clients MCP
MCP_EXTERNAL_PORT=39127  # Port principal MCP
MCP_STATUS_PORT=39128    # Port de statut/monitoring

# Configuration optionnelle pour Traefik (si vous voulez exposer MCP via Traefik)
# MCP_TRAEFIK_ENABLE=false
# MCP_TRAEFIK_HOST=mcp.openproject.tms-solutions.fr
